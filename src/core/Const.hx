package core;

class Const {
    // really big number. used for camera bounds. from deepnight/gameBase
    public static inline final RBN:Int = 0xfffffff;

    public static final SQRT2:Float = Math.sqrt(2);
}
